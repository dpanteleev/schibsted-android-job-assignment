pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal() // It's good practice to also include this
    }
}

rootProject.name = "android-job-assignment"
include ':app'